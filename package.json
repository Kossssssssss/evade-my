{
  "dependencies": {
    "three": "^0.179.1",
    "vite": "^7.1.3"
  },
  "devDependencies": {
    "@types/three": "^0.179.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add -f dist && git commit -m 'Deploy' && git subtree push --prefix dist dev gh-pages"
  }
}